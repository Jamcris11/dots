{
	match = "class_g = 'dmenu'";
	fade = false;
	animations = ({
		triggers = ["open"];
		preset = "fly-in";
		direction = "up";
		duration = 0.1;
	}, {
		triggers = ["close"];
		preset = "disappear";
		scale = 0.01;
		duration = 0.2;
	})
},
{
	match = "class_i = 'spterm'";
	fade = false;
	corner-radius = 25;
	shadow = false;
	animations = ({
		triggers = ["open", "show"];
		preset = "fly-in";
		direction = "left";
		duration = 0.1;
	}, {
		triggers = ["close", "hide"];
		preset = "fly-out";
		direction = "left";
		duration = 0.1;
	})
},
